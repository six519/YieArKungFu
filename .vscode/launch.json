{
	/*
		Install CodeLLDB extension to make it work
	*/
	"version": "0.2.0",
	"configurations": [
		{
			"type": "lldb",
			"request": "launch",
			"name": "Debug",
			"program": "${workspaceFolder}/build/kungfu",
			"args": [],
			"cwd": "${workspaceFolder}"
		}
	]
}